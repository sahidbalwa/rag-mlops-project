name: CD — Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -f infrastructure/docker/Dockerfile.api -t rag-api:latest .

      - name: Deploy (placeholder)
        run: echo "Add your AWS/GCP deploy commands here"
